name: Clementine E2E Tests Build Services
description: 'Start services required for building Clementine'

runs:
  using: "composite"
  steps:
    - name: Install Protoc
      uses: arduino/setup-protoc@v3
